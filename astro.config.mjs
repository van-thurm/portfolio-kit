// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://van-thurm.github.io',
  base: '/portfolio-kit',
  output: 'static',
  devToolbar: { enabled: false },
});
